name: naza_asset_foundry
description: Human-reviewed GPT Python codecells for local rigged 3D asset generation.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  cryptography: ^2.9.0
  file_selector: ^1.1.0
  flutter_secure_storage: ^10.3.1
  path_provider: ^2.1.6
  sqlite3: ^3.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
